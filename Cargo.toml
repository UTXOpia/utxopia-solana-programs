[workspace]
resolver = "2"
members = [
    "programs/utxopia",
    "programs/btc-light-client",
    "programs/utxopia-policy",
]
exclude = [
    "programs/chadbuffer",
    "programs/svm-tests",
    # Throwaway CU spike for the multi-chain design note. Excluded so it cannot perturb the
    # deployed crates' dependency resolution — delete the crate and this line together.
    "programs/equihash-bench"
]

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
